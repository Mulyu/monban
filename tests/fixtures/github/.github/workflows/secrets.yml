name: Secrets
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo "allowed"
        env:
          TOKEN: ${{ secrets.NPM_TOKEN }}
          GH: ${{ secrets.GITHUB_TOKEN }}
          UNKNOWN: ${{ secrets.MYSTERY_SECRET }}
